# URL of the boardgame.io server (used by the frontend)
# Default when unset: http://localhost:8000
VITE_SERVER_URL=http://localhost:8000

# Comma-separated list of allowed CORS origins for the server
# Default when unset: all origins allowed (fine for development)
ALLOWED_ORIGINS=http://localhost:5173
